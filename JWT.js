const express = required('express')
const app = express()

app.listen(250)

// now run the cmd node then >require('crypto').randomBytes(32).toString('hex')
// then copy the string and paste it in the .env file at JWT 

